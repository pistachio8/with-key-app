{
  "name": "@withkey/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo '[@withkey/domain] lint(eslint)은 web 루트 config 에 위임 — 순수 TS 게이트는 typecheck/test'",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^2.1.8"
  }
}
