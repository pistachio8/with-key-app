{
  "name": "@withkey/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo '[@withkey/domain] shell — lint(eslint)은 G6 도메인 실이동 시 배선'",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^2.1.8"
  }
}
