// /me — 프로필/알림 설정/로그아웃 화면 placeholder. 실 콘텐츠는 EVAL-0017 이후.
import { PlaceholderScreen } from "@/shared/components/placeholder-screen";

export default function MeScreen() {
  return <PlaceholderScreen title="내 정보" lines={["프로필 · 설정 — EVAL-0017"]} />;
}
