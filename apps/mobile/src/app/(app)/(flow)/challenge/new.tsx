// /challenge/new — 챌린지 생성 flow placeholder (modal presentation, 04 §3).
// create_challenge mutation 은 EVAL-0018.
import { PlaceholderScreen } from "@/shared/components/placeholder-screen";

export default function ChallengeNewScreen() {
  return <PlaceholderScreen title="챌린지 만들기" lines={["생성 flow — EVAL-0018"]} />;
}
