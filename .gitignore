# Dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Caches
.cache/
.turbo
*.tsbuildinfo

# Next.js
/.next/
/out/
/build
next-env.d.ts

# Testing
/coverage

# Vercel
.vercel

# Env (local only — never commit; use .env.example as template)
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
yarn.lock

# Tooling
.DS_Store
*.pem
*.bak

# Supabase CLI link-state (per-machine)
supabase/.temp/

# Playwright
/test-results/
/playwright-report/
/tests/e2e/.auth/

# Personal AI assistant workspace
# Keep the directory open so we can whitelist team-shared rules below.
/.claude/*
!/.claude/rules/
# Personal subtrees under .claude/rules/ (if any) should be re-ignored here.
.cursor/
.agents/
.codex/

# Personal journal (Ian's dev log — not for team)
docs/JOURNAL.md
