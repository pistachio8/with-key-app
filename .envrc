# Auto-load apps/web/.env.local on cd (used by Claude Code MCP servers in .mcp.json)
# Run `direnv allow` after editing apps/web/.env.local
set -a
[ -f apps/web/.env.local ] && source apps/web/.env.local
set +a
